project: sheets
kind: Added
body: "Write the operation `client` slots as `x-telo-ref: Http.Client` instead of the deprecated `std/http-client#Client` form, and pin the http-client import to the OCI ref `0.19.0`."
