# Generated by `telo release apply` — what each module looks like as published.
# A cache of the registry's answer, so a PR gate needs no credentials.
# `telo release verify` reconciles it against the registry.
registry: oci://ghcr.io/telorun/aws
modules:
  lambda:
    version: 0.11.0
    layers:
      manifest: sha256-H5x3LU_fr5VDH075U2KGYgngMVWy_4GH7UbdaHARqFY
  s3:
    version: 0.8.2
    layers:
      manifest: sha256-ZNlbdwGPJpS5Mdy_kB5gm_Qov5x3J-G7Z1xRPL5XHw0
