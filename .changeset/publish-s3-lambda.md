---
"@telorun/s3": patch
"@telorun/lambda": patch
---

First release of the `s3` and `lambda` modules from the connectors repo (moved from `telorun/telo`). Controllers republish with corrected MIT license and `telorun/connectors` repository metadata; the module manifests publish to OCI at `oci://ghcr.io/telorun/aws/{s3,lambda}`.
